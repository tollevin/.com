let component = ReasonReact.statelessComponent("Latest");

let make = (_children) => {
  ...component,
  render: (_self) =>
    <div id="Latest">
      /*{this.props.content.map(function (entry, index) {
        return (
          <Entry key={index} post={post}/>
        );
      })}*/
    </div>
};